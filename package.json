{
  "name": "reply-better-ai",
  "version": "1.2.3",
  "description": "Improve your messages using AI. Dynamic OpenRouter model picker. Chrome + Firefox.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --package",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
